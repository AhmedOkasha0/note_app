import 'package:flutter/material.dart';

class CustomHomeBoady extends StatelessWidget {
  const CustomHomeBoady({super.key});

  @override
  Widget build(BuildContext context) {
    return  Column(
      children: [

      ],
    );
  }
}
